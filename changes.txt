Improved ClassType creation from java.lang.Class

Fixed a bug with reading the InnerClasses attribute

Added decompilation of call chains like `new StringBuilder(...).append(...).append(...).toString()` as string concatenation.

Fixed a bug with declaring all variables in the external scope method
Added definition of the variable name by the names of fields and methods
