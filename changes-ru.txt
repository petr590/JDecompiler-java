Добавлена декомпиляция finally // не додеано

Исправлен баг с тем, что InvokespecialOperation всегда возвращал тип void.

Исправлен баг когда во вложенном блоке объявлялась переменная, а после него во внешнем блоке объявлялась другая переменная.
В этом случае компилятор кладёт обе переменные в один слот, так как они не пересекаются.

Улучшен вывод полей: теперь поля не могут объявляться через запятую, если хотя бы одному из них присваивается значение.
Добавлена фича вывода C-style массивов.

Добавлены обёртки над переменными, чтобы не переделывать уже существующий API

Добавлена декомпиляция циклов (из кода, генерируемого стандартным компилятором), распознавание for.

Добавлен класс x590.jdecompiler.context.PreDecompilationContext, для стадии preDecompilation
Добавлена стадия postDecompilation для Operation

Operation теперь интерфейс, который реализуется классом AbstractOperation. Это позволило сделать API более гибким.

Улучшена декомпиляция выражений вида `integer1 + integer2` - убраны лишние явные преобразования
Изменён метод Type#castToGeneral, нобавлены методы Type#castToGeneralNoexcept и Type#implicitCastToGeneralNoexcept
