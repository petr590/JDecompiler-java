Небольшой рефакторинг в классе Main.

Исправлена ошибка с возвращаемым типом побитовых операторов, которые могут работать с boolean (&, |, ^).

Добавлена автораспаковка типов.

Добавлено объявление переменных.

Исправлен баг, когда суперкласс для типов интерфейсов был null, теперь всегда Object.

Исправлен баг с вызовом нестатических методов: объект снимался со стека раньше аргументов.

Исправлен баг с преобразованием UncertainReferenceType

Добавлена конвертация типов (было: `(byte)(int)longValue`, стало: `(byte)longValue`)

Исправлено чтение аттрибута Code
Добавлены имена переменных из LocalVariableTable (пока только для параметров)

Добавлена generic сигнатура класса, и вообще я навёл порядок в чтении сигнатур.

Исправлен баг с импортом generic класса.
