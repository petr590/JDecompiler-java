Добавлены имена переменных в заголовок метода. Также добавлена декомпиляция varargs методов.

Улучшена система переменных: теперь имена для переменных генерируются после декомпиляции, а не при записи в поток.

Добалена поддержка атрибутов RuntimeVisibleParameterAnnotations и RuntimeInvisibleParameterAnnotations.
(правда такое ощущение, что они игнорируются компилятором - он везде вставляет RuntimeVisibleTypeAnnotations и RuntimeInvisibleTypeAnnotations).

Переписана часть кода в атрибутах с ручного чтения массива на чтение через метод readArray.

Добавлены методы writeln и writesp в класс StringifyOutputStream.


Переделана система типов. Исправлено приведение VariableCapacityintegraltype и PrimitiveType.
Немного упрощён код преобразования типов.

Добавлен метод Type.canCastTo в качестве частичной замены метода Type.isSubtypeOf.
Добавлен метод Type.reduced (в коде C++ он называется getReducedType).

Исправлена ошибка в методах DisassemblerContext.readShort и DisassemblerContext.readInt.

Отрефакторены пакеты x590.java класс.operation и x590.javaclass.instruction: классы перемещены в подпакеты.

Исправлена ошибка в DConstOperation.


Добавлен метод isScope() в класс Operation. Используется вместо проверки instanceof Scope.

Исправлен баг, когда вместо "x + 1" выводилось "x + true".

Исправлен баг с неправильной индексацией переменной, когда перед ней стоит переменная типа long или double.


Реализован пре-инкремент, пост-инкремент и укороченные операторы ("x += y" вместо "x = x + y") и для локальных переменных, и для полей.
Исправлена пара багов с ними.

Добавлено объявление полей через запятую (при условии, что у них одинаковый тип, модификаторы и все аннотации).

Исправлена ошибка при чтении аннотации со строковым значением.


... И ещё куча мелких доработок и рефакторингов ...
