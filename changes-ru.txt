Добавлена декомпиляция методов доступа к приватным членам вложенных классов, которые генерирует компилятор.
Добавлен инлайнинг методов.

Исправлен баг с переменными при инлайнинге методов.

Исправлен баг с бесконечным зацикливанием из-за того, что метод Operation#deduceType всегда возвращал true (из-за сравнения типов через `==`, а не через equals)

Проект теперь пишется в IntelliJ IDEA, а не в Eclipse, так как Eclipse всё время зависал. Причина этого так и не была выяснена.

Добавлена опция -jdk для декомпиляции классов jdk.

Исправлен тип this в типизированных классах.

Исправлен вызов generic-методов invokevirtual.

Исправлен вызов метода clone() у массивов.
