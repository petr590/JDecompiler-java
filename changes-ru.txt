Исправлен вызов конструктора this в enum-ах.
Исправлен баг с декомпиляцией абстрактных методов (модификатор abstract не выводился).

Добавлен инлайнинг массива в varargs методах (только при условии, что нет другого метода с таким же числом аргументов. Если класс недоступен, массив не инлайнится).
