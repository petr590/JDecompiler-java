Добавлены операторы && и ||, добавлена даже декомпиляция сложных условий.

Исправлен приоритет операторов в выражениях типа `!(a && b)` (было `!a && b`)
Исправлен приоритет оператора not (было `!(a)`, стало `!a`)

Исправлены некрторые javadoc-и и добавлены некоторые новые.

Исправлен баг с неверным определением сокращённых операторов там, где их не может быть.
