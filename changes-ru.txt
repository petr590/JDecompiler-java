Улучшен вывод членов класса. Теперь если в enum-е нет ничего кроме констант, не выводится точка с запятой.
Не выводится модификатор `static` для вложенных enum-ов и интерфейсов, так как они всегда статические.
При выводе синтетических полей, методов, классов, а также bridge и autogenerated методов выводится соответствующая строка в комментарии.

Исправлена декомпиляция конструкторов вложенных нестатических классов.

Исправлен порядок вывода вложенных классов.

Исправлен баг с декомпиляцией лямбд, которые захватывали локальные переменные.

Добавлена декомпиляция records

Добавлена декомпиляция `break` и `continue`
